name: gestion_salud
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.4.0
  firebase_auth: ^5.1.0
  firebase_core: ^3.15.2
  firebase_storage: ^12.4.10
  firebase_messaging: ^15.1.5
  flutter_riverpod: ^2.5.1
  riverpod: ^2.5.1
  image_picker: ^1.1.2
  intl: ^0.19.0
  geolocator: ^14.0.2
  url_launcher: ^6.3.0
  get: ^4.7.3
  pdf: ^3.11.0
  printing: ^5.13.0
  # Escáner de código de barras (inventario)
  mobile_scanner: ^5.2.3
  # Permisos
  permission_handler: ^11.3.1
  # Notificaciones locales
  flutter_local_notifications: ^18.0.0
  # Animaciones onboarding
  lottie: ^3.1.0
  # Shimmer loading
  shimmer: ^3.0.0
  # Estado de primera vez (onboarding)
  shared_preferences: ^2.3.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
